# loqio: Multi-Language Video Transcription
#### loqio uses OpenAI Whisper models to transcribe and translate audio from YouTube videos which are then matched together based on timestamps for easy comparison between languages.

## Setup:
##### Install necessary imports using: `pip install -r requirements.txt`

##### Install FFmpeg for audio processing.

##### Run FastAPI server using: `fastapi dev loqio/main.py`
